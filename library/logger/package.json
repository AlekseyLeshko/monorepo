{
  "name": "logger",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn clear && npm run compile",
    "clear": "rm -rf node_modules && rm -rf ./dist",
    "compile": "tsc",
    "lint": "eslint **/*.js",
    "start": "tsc --watch"
  }
}
